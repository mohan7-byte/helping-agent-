name: Build Gemini Live APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Accept Android SDK Licenses
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true

      - name: Scaffold Native Android App
        run: |
          mkdir -p app/src/main/java/com/gemini/live
          mkdir -p app/src/main/assets
          mkdir -p app/src/main/res/xml
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/mipmap-xxxhdpi
          mkdir -p app/src/main/res/mipmap-xxhdpi
          mkdir -p app/src/main/res/mipmap-xhdpi
          mkdir -p app/src/main/res/mipmap-hdpi
          mkdir -p app/src/main/res/mipmap-mdpi

          cp index.html app/src/main/assets/index.html

          # Fallback icon
          echo "iVBORw0KGgoAAAANSUhEUgAAAEAAAABACAYAAACqaXHeAAAATUlEQVR42u3PMQEAAAgEIPuX1hgc/LDBV9JJ6rWTAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAwBv0hQABH1mYugAAAABJRU5ErkJggg==" | base64 -d > app/src/main/res/mipmap-hdpi/ic_launcher.png
          cp app/src/main/res/mipmap-hdpi/ic_launcher.png app/src/main/res/mipmap-mdpi/ic_launcher.png
          cp app/src/main/res/mipmap-hdpi/ic_launcher.png app/src/main/res/mipmap-xhdpi/ic_launcher.png
          cp app/src/main/res/mipmap-hdpi/ic_launcher.png app/src/main/res/mipmap-xxhdpi/ic_launcher.png
          cp app/src/main/res/mipmap-hdpi/ic_launcher.png app/src/main/res/mipmap-xxxhdpi/ic_launcher.png

          if [ -f icon.png ]; then
            cp icon.png app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
            cp icon.png app/src/main/res/mipmap-xxhdpi/ic_launcher.png
            cp icon.png app/src/main/res/mipmap-xhdpi/ic_launcher.png
            cp icon.png app/src/main/res/mipmap-hdpi/ic_launcher.png
            cp icon.png app/src/main/res/mipmap-mdpi/ic_launcher.png
          fi

          cat << 'EOF' > app/src/main/res/values/strings.xml
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <string name="app_name">Voice</string>
              <string name="accessibility_desc">Enables Volume Down double-click trigger, pinpoint screen automation, and autonomous device controls.</string>
          </resources>
          EOF

          cat << 'EOF' > app/src/main/res/xml/accessibility_service_config.xml
          <?xml version="1.0" encoding="utf-8"?>
          <accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
              android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
              android:accessibilityFeedbackType="feedbackGeneric"
              android:accessibilityFlags="flagDefault|flagRequestFilterKeyEvents|flagRetrieveInteractiveWindows"
              android:canRequestFilterKeyEvents="true"
              android:canRetrieveWindowContent="true"
              android:canPerformGestures="true"
              android:description="@string/accessibility_desc"
              android:notificationTimeout="50" />
          EOF

          cat << 'EOF' > gradle.properties
          android.useAndroidX=true
          android.nonTransitiveRClass=true
          org.gradle.jvmargs=-Xmx2048m
          EOF

          cat << 'EOF' > build.gradle
          buildscript {
              repositories {
                  google()
                  mavenCentral()
              }
              dependencies {
                  classpath 'com.android.tools.build:gradle:8.2.2'
              }
          }
          allprojects {
              repositories {
                  google()
                  mavenCentral()
              }
          }
          EOF

          cat << 'EOF' > settings.gradle
          rootProject.name = "Voice"
          include ':app'
          EOF

          cat << 'EOF' > app/build.gradle
          apply plugin: 'com.android.application'

          android {
              namespace 'com.gemini.live'
              compileSdk 34

              defaultConfig {
                  applicationId "com.gemini.live"
                  minSdk 24
                  targetSdk 34
                  versionCode 13
                  versionName "2.0"
              }
              buildTypes {
                  release {
                      minifyEnabled false
                  }
              }
          }
          EOF

          cat << 'EOF' > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.INTERNET" />
              <uses-permission android:name="android.permission.RECORD_AUDIO" />
              <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
              <uses-permission android:name="android.permission.CAMERA" />
              <uses-permission android:name="android.permission.FLASHLIGHT" />
              <uses-permission android:name="android.permission.VIBRATE" />
              <uses-permission android:name="android.permission.CALL_PHONE" />
              <uses-permission android:name="android.permission.SEND_SMS" />
              <uses-permission android:name="android.permission.READ_CONTACTS" />
              <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
              <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
              <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
              <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
              <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

              <uses-feature android:name="android.hardware.camera" android:required="false" />
              <uses-feature android:name="android.hardware.camera.flash" android:required="false" />
              <uses-feature android:name="android.hardware.telephony" android:required="false" />

              <application
                  android:label="Voice"
                  android:icon="@mipmap/ic_launcher"
                  android:hardwareAccelerated="true"
                  android:usesCleartextTraffic="true"
                  android:requestLegacyExternalStorage="true"
                  android:theme="@android:style/Theme.Translucent.NoTitleBar">
                  <activity
                      android:name=".MainActivity"
                      android:exported="true"
                      android:showWhenLocked="true"
                      android:turnScreenOn="true"
                      android:launchMode="singleInstance"
                      android:configChanges="orientation|screenSize|keyboardHidden">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                      <intent-filter>
                          <action android:name="android.intent.action.ASSIST" />
                          <category android:name="android.intent.category.DEFAULT" />
                      </intent-filter>
                  </activity>

                  <service
                      android:name=".VolumeTriggerService"
                      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
                      android:exported="true">
                      <intent-filter>
                          <action android:name="android.accessibilityservice.AccessibilityService" />
                      </intent-filter>
                      <meta-data
                          android:name="android.accessibilityservice"
                          android:resource="@xml/accessibility_service_config" />
                  </service>

                  <service
                      android:name=".ScreenCaptureService"
                      android:enabled="true"
                      android:exported="false"
                      android:foregroundServiceType="mediaProjection" />
              </application>
          </manifest>
          EOF

          # ScreenCaptureService.java: 1:1 Crisp 288x592 Capture + Instant VirtualDisplay Release
          cat << 'EOF' > app/src/main/java/com/gemini/live/ScreenCaptureService.java
          package com.gemini.live;

          import android.app.Notification;
          import android.app.NotificationChannel;
          import android.app.NotificationManager;
          import android.app.Service;
          import android.content.Context;
          import android.content.Intent;
          import android.content.pm.ServiceInfo;
          import android.graphics.Bitmap;
          import android.graphics.PixelFormat;
          import android.hardware.display.DisplayManager;
          import android.hardware.display.VirtualDisplay;
          import android.media.Image;
          import android.media.ImageReader;
          import android.media.projection.MediaProjection;
          import android.media.projection.MediaProjectionManager;
          import android.os.Build;
          import android.os.IBinder;
          import android.os.SystemClock;
          import android.util.Base64;
          import android.util.DisplayMetrics;
          import android.view.WindowManager;
          import java.io.ByteArrayOutputStream;
          import java.nio.ByteBuffer;

          public class ScreenCaptureService extends Service {
              public static ScreenCaptureService instance = null;
              private MediaProjection mediaProjection;
              private int resultCode = 0;
              private Intent resultData = null;

              @Override
              public void onCreate() {
                  super.onCreate();
                  instance = this;
                  startForegroundNotification();
              }

              @Override
              public int onStartCommand(Intent intent, int flags, int startId) {
                  startForegroundNotification();
                  if (intent != null && intent.hasExtra("resultCode") && intent.hasExtra("data")) {
                      this.resultCode = intent.getIntExtra("resultCode", 0);
                      this.resultData = intent.getParcelableExtra("data");
                      initProjection();
                  }
                  return START_STICKY;
              }

              private void initProjection() {
                  try {
                      if (mediaProjection == null && resultData != null) {
                          MediaProjectionManager mpm = (MediaProjectionManager) getSystemService(Context.MEDIA_PROJECTION_SERVICE);
                          if (mpm != null) {
                              mediaProjection = mpm.getMediaProjection(resultCode, resultData);
                          }
                      }
                  } catch (Exception ignored) {}
              }

              // Captures 1:1 Native Resolution without downscaling blur
              public String captureScreenBase64() {
                  if (mediaProjection == null) {
                      initProjection();
                      if (mediaProjection == null) return null;
                  }

                  VirtualDisplay virtualDisplay = null;
                  ImageReader imageReader = null;
                  Image image = null;
                  Bitmap bitmap = null;

                  try {
                      WindowManager wm = (WindowManager) getSystemService(Context.WINDOW_SERVICE);
                      DisplayMetrics dm = new DisplayMetrics();
                      if (wm != null) wm.getDefaultDisplay().getRealMetrics(dm);
                      else dm = getResources().getDisplayMetrics();

                      // Native resolution 1:1 (crisp 288x592 text)
                      int width = dm.widthPixels;
                      int height = dm.heightPixels;

                      imageReader = ImageReader.newInstance(width, height, PixelFormat.RGBA_8888, 2);
                      virtualDisplay = mediaProjection.createVirtualDisplay(
                          "VoiceShot", width, height, dm.densityDpi,
                          DisplayManager.VIRTUAL_DISPLAY_FLAG_AUTO_MIRROR,
                          imageReader.getSurface(), null, null
                      );

                      for (int i = 0; i < 6; i++) {
                          SystemClock.sleep(50);
                          image = imageReader.acquireLatestImage();
                          if (image != null) break;
                      }

                      if (image == null) return null;

                      Image.Plane[] planes = image.getPlanes();
                      ByteBuffer buffer = planes[0].getBuffer();
                      int pixelStride = planes[0].getPixelStride();
                      int rowStride = planes[0].getRowStride();
                      int rowPadding = rowStride - pixelStride * width;

                      bitmap = Bitmap.createBitmap(width + rowPadding / pixelStride, height, Bitmap.Config.ARGB_8888);
                      bitmap.copyPixelsFromBuffer(buffer);

                      Bitmap cropped = Bitmap.createBitmap(bitmap, 0, 0, width, height);
                      ByteArrayOutputStream baos = new ByteArrayOutputStream();
                      cropped.compress(Bitmap.CompressFormat.JPEG, 75, baos);
                      return Base64.encodeToString(baos.toByteArray(), Base64.NO_WRAP);

                  } catch (Exception e) {
                      return null;
                  } finally {
                      if (bitmap != null) bitmap.recycle();
                      if (image != null) image.close();
                      if (virtualDisplay != null) virtualDisplay.release(); // Mirroring icon disappears immediately
                      if (imageReader != null) imageReader.close();
                  }
              }

              private void startForegroundNotification() {
                  String channelId = "voice_screen_capture";
                  NotificationManager nm = (NotificationManager) getSystemService(Context.NOTIFICATION_SERVICE);
                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O && nm != null) {
                      NotificationChannel chan = new NotificationChannel(channelId, "Voice Screen Capture", NotificationManager.IMPORTANCE_LOW);
                      nm.createNotificationChannel(chan);
                  }

                  Notification.Builder b = (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) 
                      ? new Notification.Builder(this, channelId) 
                      : new Notification.Builder(this);

                  Notification notif = b.setContentTitle("Voice Vision")
                      .setContentText("Screen engine ready")
                      .setSmallIcon(android.R.drawable.ic_menu_camera)
                      .build();

                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
                      startForeground(1001, notif, ServiceInfo.FOREGROUND_SERVICE_TYPE_MEDIA_PROJECTION);
                  } else {
                      startForeground(1001, notif);
                  }
              }

              @Override
              public void onDestroy() {
                  instance = null;
                  if (mediaProjection != null) mediaProjection.stop();
                  super.onDestroy();
              }

              @Override
              public IBinder onBind(Intent intent) { return null; }
          }
          EOF

          # VolumeTriggerService.java: Sniper Normalization, Magnetic Snap, Long-Press & Interactive Catalog
          cat << 'EOF' > app/src/main/java/com/gemini/live/VolumeTriggerService.java
          package com.gemini.live;

          import android.accessibilityservice.AccessibilityService;
          import android.accessibilityservice.GestureDescription;
          import android.content.ClipData;
          import android.content.ClipboardManager;
          import android.content.Context;
          import android.content.Intent;
          import android.graphics.Path;
          import android.graphics.Rect;
          import android.os.Build;
          import android.os.Bundle;
          import android.os.Handler;
          import android.os.Looper;
          import android.os.SystemClock;
          import android.os.VibrationEffect;
          import android.os.Vibrator;
          import android.util.DisplayMetrics;
          import android.view.KeyEvent;
          import android.view.WindowManager;
          import android.view.accessibility.AccessibilityEvent;
          import android.view.accessibility.AccessibilityNodeInfo;
          import android.widget.Toast;
          import java.util.ArrayList;
          import java.util.List;

          public class VolumeTriggerService extends AccessibilityService {
              public static VolumeTriggerService instance = null;
              private long lastVolumeDownTime = 0;
              private static final long DOUBLE_PRESS_INTERVAL = 450;
              private Handler mainHandler = new Handler(Looper.getMainLooper());

              @Override
              public void onServiceConnected() {
                  super.onServiceConnected();
                  instance = this;
              }

              @Override
              public void onDestroy() {
                  instance = null;
                  super.onDestroy();
              }

              private void showToast(String msg) {
                  mainHandler.post(() -> {
                      try {
                          Toast.makeText(getApplicationContext(), msg, Toast.LENGTH_SHORT).show();
                      } catch (Exception ignored) {}
                  });
              }

              @Override
              public void onAccessibilityEvent(AccessibilityEvent event) {
                  if (event == null) return;
                  int type = event.getEventType();
                  if (type == AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED || type == AccessibilityEvent.TYPE_WINDOW_CONTENT_CHANGED) {
                      autoApproveDialogs();
                  }
              }

              private void autoApproveDialogs() {
                  AccessibilityNodeInfo root = getRootInActiveWindow();
                  if (root == null) return;
                  findAndClickApprovalButton(root);
              }

              private boolean findAndClickApprovalButton(AccessibilityNodeInfo node) {
                  if (node == null) return false;
                  CharSequence text = node.getText();
                  if (text != null) {
                      String t = text.toString().toLowerCase().trim();
                      if (t.equals("start now") || t.equals("allow") || t.equals("while using the app")) {
                          return node.performAction(AccessibilityNodeInfo.ACTION_CLICK);
                      }
                  }
                  String id = node.getViewIdResourceName();
                  if (id != null && (id.endsWith("button1") || id.endsWith("permission_allow_button"))) {
                      CharSequence btnText = node.getText();
                      if (btnText == null || !btnText.toString().toLowerCase().contains("cancel")) {
                          return node.performAction(AccessibilityNodeInfo.ACTION_CLICK);
                      }
                  }
                  for (int i = 0; i < node.getChildCount(); i++) {
                      if (findAndClickApprovalButton(node.getChild(i))) return true;
                  }
                  return false;
              }

              @Override
              public void onInterrupt() {}

              @Override
              protected boolean onKeyEvent(KeyEvent event) {
                  int keyCode = event.getKeyCode();
                  int action = event.getAction();

                  if (keyCode == KeyEvent.KEYCODE_VOLUME_DOWN && action == KeyEvent.ACTION_DOWN) {
                      long now = SystemClock.uptimeMillis();
                      if (now - lastVolumeDownTime < DOUBLE_PRESS_INTERVAL) {
                          lastVolumeDownTime = 0;
                          launchAssistantWithHaptics();
                          return true;
                      } else {
                          lastVolumeDownTime = now;
                      }
                  }
                  return super.onKeyEvent(event);
              }

              private void launchAssistantWithHaptics() {
                  try {
                      Vibrator v = (Vibrator) getSystemService(Context.VIBRATOR_SERVICE);
                      if (v != null) {
                          if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                              v.vibrate(VibrationEffect.createWaveform(new long[]{0, 50, 70, 50}, -1));
                          } else {
                              v.vibrate(new long[]{0, 50, 70, 50}, -1);
                          }
                      }
                  } catch (Exception ignored) {}

                  Intent intent = new Intent(this, MainActivity.class);
                  intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK 
                                | Intent.FLAG_ACTIVITY_SINGLE_TOP 
                                | Intent.FLAG_ACTIVITY_REORDER_TO_FRONT);
                  startActivity(intent);
              }

              // Calibrated for 288x592: Direct Pixels + Small Button Snap Only (No Containers)
              public boolean tapCoordinates(int x, int y) {
                  if (Build.VERSION.SDK_INT < Build.VERSION_CODES.N) return false;

                  WindowManager wm = (WindowManager) getSystemService(Context.WINDOW_SERVICE);
                  DisplayMetrics dm = new DisplayMetrics();
                  if (wm != null) wm.getDefaultDisplay().getRealMetrics(dm);
                  else dm = getResources().getDisplayMetrics();

                  int targetX = x;
                  int targetY = y;

                  // If Gemini sends normalized numbers (greater than screen size), scale down to 288x592
                  if (x > dm.widthPixels || y > dm.heightPixels) {
                      targetX = Math.round((x / 1000.0f) * dm.widthPixels);
                      targetY = Math.round((y / 1000.0f) * dm.heightPixels);
                  }

                  // Magnetic Snap: ONLY snap to small leaf buttons (never giant containers!)
                  AccessibilityNodeInfo root = getRootInActiveWindow();
                  if (root != null) {
                      Rect snapped = findNearbyElementBounds(root, targetX, targetY, dm.widthPixels, 25);
                      if (snapped != null) {
                          targetX = snapped.centerX();
                          targetY = snapped.centerY();
                      }
                  }

                  targetX = Math.max(5, Math.min(targetX, dm.widthPixels - 5));
                  targetY = Math.max(5, Math.min(targetY, dm.heightPixels - 5));

                  Path clickPath = new Path();
                  clickPath.moveTo(targetX, targetY);
                  clickPath.lineTo(targetX, targetY + 2);

                  GestureDescription.StrokeDescription stroke = new GestureDescription.StrokeDescription(clickPath, 0, 120);
                  GestureDescription.Builder builder = new GestureDescription.Builder();
                  builder.addStroke(stroke);

                  final int finalX = targetX;
                  final int finalY = targetY;

                  return dispatchGesture(builder.build(), new GestureResultCallback() {
                      @Override
                      public void onCompleted(GestureDescription gestureDescription) {
                          super.onCompleted(gestureDescription);
                          showToast("✓ Tapped (" + finalX + ", " + finalY + ")");
                      }
                  }, null);
              }

              // Hardware Long-Press (700ms) for Text Selection & Context Menus
              public boolean longPress(int x, int y) {
                  if (Build.VERSION.SDK_INT < Build.VERSION_CODES.N) return false;

                  WindowManager wm = (WindowManager) getSystemService(Context.WINDOW_SERVICE);
                  DisplayMetrics dm = new DisplayMetrics();
                  if (wm != null) wm.getDefaultDisplay().getRealMetrics(dm);
                  else dm = getResources().getDisplayMetrics();

                  int targetX = (x > dm.widthPixels) ? Math.round((x / 1000.0f) * dm.widthPixels) : x;
                  int targetY = (y > dm.heightPixels) ? Math.round((y / 1000.0f) * dm.heightPixels) : y;

                  Path path = new Path();
                  path.moveTo(targetX, targetY);
                  path.lineTo(targetX, targetY + 1);

                  GestureDescription.StrokeDescription stroke = new GestureDescription.StrokeDescription(path, 0, 700);
                  GestureDescription.Builder builder = new GestureDescription.Builder();
                  builder.addStroke(stroke);

                  final int fX = targetX;
                  final int fY = targetY;

                  return dispatchGesture(builder.build(), new GestureResultCallback() {
                      @Override
                      public void onCompleted(GestureDescription gestureDescription) {
                          super.onCompleted(gestureDescription);
                          showToast("✓ Long-pressed (" + fX + ", " + fY + ")");
                      }
                  }, null);
              }

              // Rejects containers: only snaps to small clickable icons/buttons (max 45% screen width)
              private Rect findNearbyElementBounds(AccessibilityNodeInfo node, int x, int y, int screenWidth, int radius) {
                  if (node == null) return null;
                  if (node.isClickable()) {
                      Rect r = new Rect();
                      node.getBoundsInScreen(r);
                      // Strict button check: must be smaller than 45% of screen width and max 120px tall
                      boolean isSmallButton = r.width() > 5 && r.width() < (screenWidth * 0.45f) && r.height() < 120;
                      if (isSmallButton) {
                          if (r.contains(x, y)) return r;
                          int dx = x - r.centerX();
                          int dy = y - r.centerY();
                          if (Math.sqrt(dx * dx + dy * dy) <= radius) return r;
                      }
                  }
                  for (int i = 0; i < node.getChildCount(); i++) {
                      Rect found = findNearbyElementBounds(node.getChild(i), x, y, screenWidth, radius);
                      if (found != null) return found;
                  }
                  return null;
              }

              public boolean swipe(int startX, int startY, int endX, int endY, int durationMs) {
                  if (Build.VERSION.SDK_INT < Build.VERSION_CODES.N) return false;
                  Path path = new Path();
                  path.moveTo(startX, startY);
                  path.lineTo(endX, endY);
                  GestureDescription.StrokeDescription stroke = new GestureDescription.StrokeDescription(path, 0, Math.max(100, durationMs));
                  GestureDescription.Builder builder = new GestureDescription.Builder();
                  builder.addStroke(stroke);
                  return dispatchGesture(builder.build(), null, null);
              }

              public boolean scroll(String direction) {
                  WindowManager wm = (WindowManager) getSystemService(Context.WINDOW_SERVICE);
                  DisplayMetrics dm = new DisplayMetrics();
                  if (wm != null) wm.getDefaultDisplay().getRealMetrics(dm);
                  else dm = getResources().getDisplayMetrics();

                  int midX = dm.widthPixels / 2;
                  int h = dm.heightPixels;
                  if ("up".equalsIgnoreCase(direction)) {
                      return swipe(midX, (int)(h * 0.3), midX, (int)(h * 0.8), 300);
                  } else {
                      return swipe(midX, (int)(h * 0.8), midX, (int)(h * 0.3), 300);
                  }
              }

              // Returns visible text AND interactive candidate elements with 0-1000 coordinates
              public String readScreenText() {
                  AccessibilityNodeInfo root = getRootInActiveWindow();
                  if (root == null) return "No active window detected.";

                  WindowManager wm = (WindowManager) getSystemService(Context.WINDOW_SERVICE);
                  DisplayMetrics dm = new DisplayMetrics();
                  if (wm != null) wm.getDefaultDisplay().getRealMetrics(dm);
                  else dm = getResources().getDisplayMetrics();

                  StringBuilder catalog = new StringBuilder();
                  catalog.append("=== INTERACTIVE ELEMENTS (COORDINATES 0-1000) ===\n");
                  List<String> elements = new ArrayList<>();
                  extractInteractiveElements(root, elements, dm, 0);
                  if (elements.isEmpty()) {
                      catalog.append("No labeled buttons detected.\n");
                  } else {
                      for (String el : elements) catalog.append(el).append("\n");
                  }

                  catalog.append("\n=== SCREEN TEXT CONTENT ===\n");
                  StringBuilder textSb = new StringBuilder();
                  extractTextRecursive(root, textSb, 0);
                  catalog.append(textSb.toString().trim());

                  return catalog.toString();
              }

              private void extractInteractiveElements(AccessibilityNodeInfo node, List<String> list, DisplayMetrics dm, int depth) {
                  if (node == null || depth > 25 || list.size() >= 25) return;
                  if (node.isClickable() || node.isEditable()) {
                      CharSequence text = node.getText();
                      CharSequence desc = node.getContentDescription();
                      String label = text != null ? text.toString().trim() : (desc != null ? desc.toString().trim() : "");
                      if (!label.isEmpty()) {
                          Rect r = new Rect();
                          node.getBoundsInScreen(r);
                          if (r.width() > 10 && r.height() > 10) {
                              int normX = Math.round((r.centerX() / (float) dm.widthPixels) * 1000);
                              int normY = Math.round((r.centerY() / (float) dm.heightPixels) * 1000);
                              String type = node.isEditable() ? "Input" : "Button";
                              list.add("[" + type + "] \"" + label + "\" at coordinates (" + normX + ", " + normY + ")");
                          }
                      }
                  }
                  for (int i = 0; i < node.getChildCount(); i++) {
                      extractInteractiveElements(node.getChild(i), list, dm, depth + 1);
                  }
              }

              private void extractTextRecursive(AccessibilityNodeInfo node, StringBuilder sb, int depth) {
                  if (node == null || depth > 25) return;
                  CharSequence text = node.getText();
                  if (text != null && text.length() > 0) {
                      sb.append(text).append("\n");
                  } else {
                      CharSequence desc = node.getContentDescription();
                      if (desc != null && desc.length() > 0) {
                          sb.append(desc).append("\n");
                      }
                  }
                  for (int i = 0; i < node.getChildCount(); i++) {
                      extractTextRecursive(node.getChild(i), sb, depth + 1);
                  }
              }

              public boolean tapElement(String label) {
                  String target = label.toLowerCase().trim();
                  AccessibilityNodeInfo root = getRootInActiveWindow();
                  if (root == null) return false;

                  List<AccessibilityNodeInfo> matches = new ArrayList<>();
                  collectMatches(root, target, matches, 0);
                  if (matches.isEmpty()) return false;

                  AccessibilityNodeInfo bestNode = null;
                  int minArea = Integer.MAX_VALUE;
                  Rect r = new Rect();
                  for (AccessibilityNodeInfo n : matches) {
                      n.getBoundsInScreen(r);
                      int area = r.width() * r.height();
                      if (area > 0 && area < minArea) {
                          minArea = area;
                          bestNode = n;
                      }
                  }

                  if (bestNode != null) {
                      bestNode.getBoundsInScreen(r);
                      return tapCoordinates(r.centerX(), r.centerY());
                  }
                  return false;
              }

              private void collectMatches(AccessibilityNodeInfo node, String target, List<AccessibilityNodeInfo> list, int depth) {
                  if (node == null || depth > 25) return;
                  CharSequence text = node.getText();
                  CharSequence desc = node.getContentDescription();
                  boolean match = (text != null && text.toString().toLowerCase().contains(target)) ||
                                  (desc != null && desc.toString().toLowerCase().contains(target));
                  if (match) list.add(node);
                  for (int i = 0; i < node.getChildCount(); i++) {
                      collectMatches(node.getChild(i), target, list, depth + 1);
                  }
              }

              // Multi-Stage Typing Engine: Protects blinking cursor & prevents line shifting
              public boolean typeText(String text) {
                  AccessibilityNodeInfo root = getRootInActiveWindow();
                  if (root == null) return false;

                  AccessibilityNodeInfo focused = root.findFocus(AccessibilityNodeInfo.FOCUS_INPUT);
                  if (focused == null) focused = findFocusedNode(root, 0);

                  if (focused != null) {
                      return pasteDirectly(focused, text);
                  }

                  AccessibilityNodeInfo editable = findFirstEditable(root, 0);
                  if (editable != null) {
                      Rect rect = new Rect();
                      editable.getBoundsInScreen(rect);
                      if (rect.width() > 0 && rect.height() > 0) {
                          tapCoordinates(rect.centerX(), rect.centerY());
                          SystemClock.sleep(200);
                      }
                      root = getRootInActiveWindow();
                      if (root != null) {
                          AccessibilityNodeInfo nowFocused = root.findFocus(AccessibilityNodeInfo.FOCUS_INPUT);
                          if (nowFocused != null) return pasteDirectly(nowFocused, text);
                      }
                      return pasteDirectly(editable, text);
                  }
                  return false;
              }

              private AccessibilityNodeInfo findFocusedNode(AccessibilityNodeInfo node, int depth) {
                  if (node == null || depth > 25) return null;
                  if (node.isFocused()) return node;
                  for (int i = 0; i < node.getChildCount(); i++) {
                      AccessibilityNodeInfo res = findFocusedNode(node.getChild(i), depth + 1);
                      if (res != null) return res;
                  }
                  return null;
              }

              private boolean pasteDirectly(AccessibilityNodeInfo node, String text) {
                  try {
                      ClipboardManager cm = (ClipboardManager) getSystemService(Context.CLIPBOARD_SERVICE);
                      if (cm != null) {
                          ClipData clip = ClipData.newPlainText("jarvis_input", text);
                          cm.setPrimaryClip(clip);
                      }
                  } catch (Exception ignored) {}

                  boolean ok = node.performAction(AccessibilityNodeInfo.ACTION_PASTE);
                  if (!ok) {
                      Bundle args = new Bundle();
                      args.putCharSequence(AccessibilityNodeInfo.ACTION_ARGUMENT_SET_TEXT_CHARSEQUENCE, text);
                      ok = node.performAction(AccessibilityNodeInfo.ACTION_SET_TEXT, args);
                  }

                  if (ok) showToast("✓ Inserted text");
                  return ok;
              }

              private AccessibilityNodeInfo findFirstEditable(AccessibilityNodeInfo node, int depth) {
                  if (node == null || depth > 25) return null;
                  if (node.isEditable()) return node;
                  for (int i = 0; i < node.getChildCount(); i++) {
                      AccessibilityNodeInfo found = findFirstEditable(node.getChild(i), depth + 1);
                      if (found != null) return found;
                  }
                  return null;
              }
          }
          EOF

          # MainActivity.java: Playbook File Persistence, My Files Intent & Full Device Bridge
          cat << 'EOF' > app/src/main/java/com/gemini/live/MainActivity.java
          package com.gemini.live;

          import android.Manifest;
          import android.accessibilityservice.AccessibilityService;
          import android.app.Activity;
          import android.app.SearchManager;
          import android.content.ContentResolver;
          import android.content.Context;
          import android.content.Intent;
          import android.content.pm.ApplicationInfo;
          import android.content.pm.PackageManager;
          import android.database.Cursor;
          import android.graphics.Color;
          import android.graphics.drawable.ColorDrawable;
          import android.hardware.camera2.CameraManager;
          import android.media.AudioManager;
          import android.media.projection.MediaProjectionManager;
          import android.net.Uri;
          import android.os.Build;
          import android.os.Bundle;
          import android.os.Environment;
          import android.os.StrictMode;
          import android.provider.ContactsContract;
          import android.telephony.SmsManager;
          import android.view.KeyEvent;
          import android.view.View;
          import android.view.WindowManager;
          import android.webkit.JavascriptInterface;
          import android.webkit.PermissionRequest;
          import android.webkit.WebChromeClient;
          import android.webkit.WebSettings;
          import android.webkit.WebView;
          import android.widget.Toast;
          import org.json.JSONArray;
          import org.json.JSONObject;
          import java.io.File;
          import java.io.FileWriter;
          import java.util.List;

          public class MainActivity extends Activity {
              public static MainActivity instance = null;
              private static final int REQUEST_MEDIA_PROJECTION = 202;
              private WebView webView;
              private MediaProjectionManager projectionManager;
              private Toast currentToast = null;

              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  instance = this;

                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O_MR1) {
                      setShowWhenLocked(true);
                      setTurnScreenOn(true);
                  } else {
                      getWindow().addFlags(WindowManager.LayoutParams.FLAG_SHOW_WHEN_LOCKED
                              | WindowManager.LayoutParams.FLAG_TURN_SCREEN_ON);
                  }

                  getWindow().setBackgroundDrawable(new ColorDrawable(Color.TRANSPARENT));

                  String[] perms = {
                      Manifest.permission.RECORD_AUDIO,
                      Manifest.permission.CAMERA,
                      Manifest.permission.CALL_PHONE,
                      Manifest.permission.SEND_SMS,
                      Manifest.permission.READ_CONTACTS,
                      Manifest.permission.READ_EXTERNAL_STORAGE,
                      Manifest.permission.WRITE_EXTERNAL_STORAGE
                  };
                  for (String p : perms) {
                      if (checkSelfPermission(p) != PackageManager.PERMISSION_GRANTED) {
                          requestPermissions(perms, 101);
                          break;
                      }
                  }

                  projectionManager = (MediaProjectionManager) getSystemService(Context.MEDIA_PROJECTION_SERVICE);

                  webView = new WebView(this);
                  webView.setBackgroundColor(Color.TRANSPARENT);
                  webView.setLayerType(View.LAYER_TYPE_HARDWARE, null);
                  setContentView(webView);

                  WebSettings s = webView.getSettings();
                  s.setJavaScriptEnabled(true);
                  s.setDomStorageEnabled(true);
                  s.setDatabaseEnabled(true);
                  s.setMediaPlaybackRequiresUserGesture(false);
                  s.setAllowFileAccess(true);
                  s.setAllowContentAccess(true);

                  webView.addJavascriptInterface(new Object() {
                      @JavascriptInterface
                      public void minimize() { runOnUiThread(() -> moveTaskToBack(true)); }

                      @JavascriptInterface
                      public void closeApp() { runOnUiThread(() -> finish()); }

                      @JavascriptInterface
                      public void showTaskProgress(int step, String action) {
                          runOnUiThread(() -> {
                              try {
                                  if (currentToast != null) currentToast.cancel();
                                  currentToast = Toast.makeText(MainActivity.this, "⚡ [" + step + "] " + action, Toast.LENGTH_SHORT);
                                  currentToast.show();
                              } catch (Exception ignored) {}
                          });
                      }

                      @JavascriptInterface
                      public void openAccessibilitySettings() {
                          runOnUiThread(() -> {
                              Intent intent = new Intent(android.provider.Settings.ACTION_ACCESSIBILITY_SETTINGS);
                              intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                              startActivity(intent);
                          });
                      }

                      @JavascriptInterface
                      public void requestScreenCapturePermission() {
                          runOnUiThread(() -> requestScreenCapture());
                      }

                      @JavascriptInterface
                      public String captureScreen() {
                          if (ScreenCaptureService.instance == null) {
                              runOnUiThread(() -> requestScreenCapture());
                              return "Screen capture initializing. Please retry in 1 second.";
                          }
                          String b64 = ScreenCaptureService.instance.captureScreenBase64();
                          return b64 != null ? b64 : "Failed to capture frame.";
                      }

                      // Saves rules to physical file: /sdcard/Documents/Voice/rules.json
                      @JavascriptInterface
                      public boolean saveRulesToFile(String jsonString) {
                          try {
                              File dir = new File(Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_DOCUMENTS), "Voice");
                              if (!dir.exists()) dir.mkdirs();
                              File file = new File(dir, "rules.json");
                              FileWriter writer = new FileWriter(file, false);
                              writer.write(jsonString);
                              writer.flush();
                              writer.close();
                              runOnUiThread(() -> Toast.makeText(MainActivity.this, "Rules synced to Documents/Voice/rules.json", Toast.LENGTH_SHORT).show());
                              return true;
                          } catch (Exception e) {
                              return false;
                          }
                      }

                      // 1-Tap launch into Samsung My Files or universal text editor
                      @JavascriptInterface
                      public void openRulesInFileManager() {
                          runOnUiThread(() -> {
                              try {
                                  File dir = new File(Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_DOCUMENTS), "Voice");
                                  if (!dir.exists()) dir.mkdirs();
                                  File file = new File(dir, "rules.json");

                                  StrictMode.VmPolicy.Builder builder = new StrictMode.VmPolicy.Builder();
                                  StrictMode.setVmPolicy(builder.build());

                                  Intent intent = new Intent(Intent.ACTION_VIEW);
                                  intent.setDataAndType(Uri.fromFile(file), "application/json");
                                  intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                                  startActivity(Intent.createChooser(intent, "Open Rules in..."));
                              } catch (Exception e) {
                                  Toast.makeText(MainActivity.this, "Cannot open: " + e.getMessage(), Toast.LENGTH_SHORT).show();
                              }
                          });
                      }

                      @JavascriptInterface
                      public String createNote(String text) {
                          try {
                              Intent intent = new Intent(Intent.ACTION_SEND);
                              intent.setType("text/plain");
                              intent.putExtra(Intent.EXTRA_TEXT, text);
                              intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                              startActivity(Intent.createChooser(intent, "Create Note"));
                              return "Opened note composer with text.";
                          } catch (Exception e) {
                              return "Failed to create note: " + e.getMessage();
                          }
                      }

                      @JavascriptInterface
                      public String searchContacts(String query) {
                          try {
                              ContentResolver cr = getContentResolver();
                              Uri uri = ContactsContract.CommonDataKinds.Phone.CONTENT_URI;
                              String selection = ContactsContract.CommonDataKinds.Phone.DISPLAY_NAME + " LIKE ?";
                              String[] selectionArgs = new String[]{"%" + query.trim() + "%"};
                              Cursor cursor = cr.query(uri, new String[]{
                                  ContactsContract.CommonDataKinds.Phone.DISPLAY_NAME,
                                  ContactsContract.CommonDataKinds.Phone.NUMBER
                              }, selection, selectionArgs, null);

                              if (cursor != null) {
                                  JSONArray results = new JSONArray();
                                  while (cursor.moveToNext()) {
                                      JSONObject obj = new JSONObject();
                                      obj.put("name", cursor.getString(0));
                                      obj.put("number", cursor.getString(1));
                                      results.put(obj);
                                      if (results.length() >= 5) break;
                                  }
                                  cursor.close();
                                  if (results.length() > 0) return results.toString();
                              }
                          } catch (Exception e) { return "Error searching contacts: " + e.getMessage(); }
                          return "No contacts found matching: " + query;
                      }

                      @JavascriptInterface
                      public String searchYouTube(String query) {
                          try {
                              Intent intent = new Intent(Intent.ACTION_SEARCH);
                              intent.setPackage("com.google.android.youtube");
                              intent.putExtra("query", query);
                              intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                              startActivity(intent);
                              return "Opened YouTube searching for " + query;
                          } catch (Exception e) {
                              Intent webIntent = new Intent(Intent.ACTION_VIEW, Uri.parse("https://www.youtube.com/results?search_query=" + Uri.encode(query)));
                              webIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                              startActivity(webIntent);
                              return "Opened YouTube in browser for " + query;
                          }
                      }

                      @JavascriptInterface
                      public String searchWeb(String query) {
                          try {
                              Intent intent = new Intent(Intent.ACTION_WEB_SEARCH);
                              intent.putExtra(SearchManager.QUERY, query);
                              intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                              startActivity(intent);
                              return "Searching web for " + query;
                          } catch (Exception e) {
                              Intent browserIntent = new Intent(Intent.ACTION_VIEW, Uri.parse("https://www.google.com/search?q=" + Uri.encode(query)));
                              browserIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                              startActivity(browserIntent);
                              return "Opened Google Search for " + query;
                          }
                      }

                      @JavascriptInterface
                      public String openWhatsAppChat(String phoneNumber, String message) {
                          try {
                              String cleanPhone = phoneNumber.replaceAll("[^0-9+]", "");
                              if (cleanPhone.startsWith("+")) cleanPhone = cleanPhone.substring(1);
                              String url = "https://api.whatsapp.com/send?phone=" + cleanPhone + "&text=" + Uri.encode(message);
                              Intent intent = new Intent(Intent.ACTION_VIEW, Uri.parse(url));
                              intent.setPackage("com.whatsapp");
                              intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                              startActivity(intent);
                              return "Opened WhatsApp chat for " + cleanPhone;
                          } catch (Exception e) {
                              return "Failed to open WhatsApp: " + e.getMessage();
                          }
                      }

                      @JavascriptInterface
                      public String tapCoordinates(int x, int y) {
                          if (VolumeTriggerService.instance == null) return "Accessibility Service required.";
                          boolean ok = VolumeTriggerService.instance.tapCoordinates(x, y);
                          return ok ? "Tapped at (" + x + ", " + y + ")" : "Failed to tap coordinates.";
                      }

                      @JavascriptInterface
                      public String longPress(int x, int y) {
                          if (VolumeTriggerService.instance == null) return "Accessibility Service required.";
                          boolean ok = VolumeTriggerService.instance.longPress(x, y);
                          return ok ? "Long-pressed at (" + x + ", " + y + ")" : "Failed to long-press.";
                      }

                      @JavascriptInterface
                      public String swipe(int x1, int y1, int x2, int y2, int duration) {
                          if (VolumeTriggerService.instance == null) return "Accessibility Service required.";
                          boolean ok = VolumeTriggerService.instance.swipe(x1, y1, x2, y2, duration);
                          return ok ? "Swiped successfully." : "Swipe failed.";
                      }

                      @JavascriptInterface
                      public String scroll(String direction) {
                          if (VolumeTriggerService.instance == null) return "Accessibility Service required.";
                          boolean ok = VolumeTriggerService.instance.scroll(direction);
                          return ok ? "Scrolled " + direction : "Scroll failed.";
                      }

                      @JavascriptInterface
                      public String toggleFlashlight(boolean state) {
                          try {
                              CameraManager cm = (CameraManager) getSystemService(Context.CAMERA_SERVICE);
                              if (cm != null) {
                                  String id = cm.getCameraIdList()[0];
                                  cm.setTorchMode(id, state);
                                  return "Flashlight set to " + (state ? "ON" : "OFF");
                              }
                          } catch (Exception e) { return "Flashlight error: " + e.getMessage(); }
                          return "CameraManager unavailable.";
                      }

                      @JavascriptInterface
                      public String setVolume(int percent) {
                          try {
                              AudioManager am = (AudioManager) getSystemService(Context.AUDIO_SERVICE);
                              if (am != null) {
                                  int max = am.getStreamMaxVolume(AudioManager.STREAM_MUSIC);
                                  int target = Math.round(max * (Math.max(0, Math.min(100, percent)) / 100.0f));
                                  am.setStreamVolume(AudioManager.STREAM_MUSIC, target, AudioManager.FLAG_SHOW_UI);
                                  return "Volume set to " + percent + "%";
                              }
                          } catch (Exception e) { return "Volume error: " + e.getMessage(); }
                          return "AudioManager unavailable.";
                      }

                      @JavascriptInterface
                      public String navigateSystem(String action) {
                          if (VolumeTriggerService.instance == null) return "Accessibility Service not enabled.";
                          int globalAction;
                          switch (action.toLowerCase()) {
                              case "back": globalAction = AccessibilityService.GLOBAL_ACTION_BACK; break;
                              case "recents": globalAction = AccessibilityService.GLOBAL_ACTION_RECENTS; break;
                              case "notifications": globalAction = AccessibilityService.GLOBAL_ACTION_NOTIFICATIONS; break;
                              case "quick_settings": globalAction = AccessibilityService.GLOBAL_ACTION_QUICK_SETTINGS; break;
                              case "lock": globalAction = AccessibilityService.GLOBAL_ACTION_LOCK_SCREEN; break;
                              case "home":
                              default: globalAction = AccessibilityService.GLOBAL_ACTION_HOME; break;
                          }
                          boolean ok = VolumeTriggerService.instance.performGlobalAction(globalAction);
                          return ok ? "Triggered " + action : "Action failed.";
                      }

                      @JavascriptInterface
                      public String openApp(String appName) {
                          PackageManager pm = getPackageManager();
                          List<ApplicationInfo> apps = pm.getInstalledApplications(PackageManager.GET_META_DATA);
                          for (ApplicationInfo app : apps) {
                              String label = pm.getApplicationLabel(app).toString().toLowerCase();
                              if (label.contains(appName.toLowerCase().trim())) {
                                  Intent launchIntent = pm.getLaunchIntentForPackage(app.packageName);
                                  if (launchIntent != null) {
                                      launchIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                                      startActivity(launchIntent);
                                      return "Opened " + label;
                                  }
                              }
                          }
                          return "Could not find an app named " + appName;
                      }

                      @JavascriptInterface
                      public String makePhoneCall(String phoneNumber) {
                          try {
                              Intent intent = new Intent(Intent.ACTION_CALL, Uri.parse("tel:" + phoneNumber.trim()));
                              intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                              startActivity(intent);
                              return "Calling " + phoneNumber;
                          } catch (Exception e) { return "Call failed: " + e.getMessage(); }
                      }

                      @JavascriptInterface
                      public String sendSms(String phoneNumber, String message) {
                          try {
                              SmsManager sm = SmsManager.getDefault();
                              sm.sendTextMessage(phoneNumber.trim(), null, message, null, null);
                              return "SMS sent to " + phoneNumber;
                          } catch (Exception e) { return "SMS failed: " + e.getMessage(); }
                      }

                      @JavascriptInterface
                      public String readScreenText() {
                          if (VolumeTriggerService.instance == null) return "Accessibility Service required to read screen.";
                          return VolumeTriggerService.instance.readScreenText();
                      }

                      @JavascriptInterface
                      public String tapElement(String label) {
                          if (VolumeTriggerService.instance == null) return "Accessibility Service required to tap elements.";
                          boolean ok = VolumeTriggerService.instance.tapElement(label);
                          return ok ? "Tapped " + label : "Could not find element: " + label;
                      }

                      @JavascriptInterface
                      public String typeText(String text) {
                          if (VolumeTriggerService.instance == null) return "Accessibility Service required to type text.";
                          boolean ok = VolumeTriggerService.instance.typeText(text);
                          return ok ? "Typed: " + text : "Could not find an active editable field to type in.";
                      }
                  }, "AndroidBridge");

                  webView.setWebChromeClient(new WebChromeClient() {
                      @Override
                      public void onPermissionRequest(final PermissionRequest request) {
                          runOnUiThread(() -> request.grant(request.getResources()));
                      }
                  });

                  webView.loadUrl("file:///android_asset/index.html");
              }

              public void requestScreenCapture() {
                  if (ScreenCaptureService.instance != null) return;
                  if (projectionManager != null) {
                      startActivityForResult(projectionManager.createScreenCaptureIntent(), REQUEST_MEDIA_PROJECTION);
                  }
              }

              @Override
              protected void onActivityResult(int requestCode, int resultCode, Intent data) {
                  if (requestCode == REQUEST_MEDIA_PROJECTION && resultCode == Activity.RESULT_OK && data != null) {
                      Intent serviceIntent = new Intent(this, ScreenCaptureService.class);
                      serviceIntent.putExtra("resultCode", resultCode);
                      serviceIntent.putExtra("data", data);
                      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                          startForegroundService(serviceIntent);
                      } else {
                          startService(serviceIntent);
                      }
                  }
                  super.onActivityResult(requestCode, resultCode, data);
              }

              @Override
              protected void onPause() {
                  super.onPause();
                  if (webView != null) webView.resumeTimers();
              }

              @Override
              protected void onStop() {
                  super.onStop();
                  if (webView != null) webView.resumeTimers();
              }

              @Override
              protected void onDestroy() {
                  instance = null;
                  super.onDestroy();
              }

              @Override
              protected void onNewIntent(Intent intent) {
                  super.onNewIntent(intent);
                  setIntent(intent);

                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O_MR1) {
                      setShowWhenLocked(true);
                      setTurnScreenOn(true);
                  }

                  if (webView != null) {
                      webView.resumeTimers();
                      webView.evaluateJavascript("if (typeof autoConnectOnWake === 'function') autoConnectOnWake();", null);
                  }
              }

              @Override
              public boolean onKeyDown(int keyCode, KeyEvent event) {
                  if (keyCode == KeyEvent.KEYCODE_BACK) {
                      moveTaskToBack(true);
                      return true;
                  }
                  return super.onKeyDown(keyCode, event);
              }
          }
          EOF

      - name: Generate Gradle Wrapper
        run: gradle wrapper --gradle-version 8.5

      - name: Compile Debug APK
        run: ./gradlew assembleDebug

      - name: Upload APK as Direct Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Voice-APK
          path: app/build/outputs/apk/debug/app-debug.apk

      - name: Create Release & Upload APK
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete v2.0 --yes || true
          git tag -d v2.0 || true
          gh release create v2.0 app/build/outputs/apk/debug/app-debug.apk \
            --title "Voice v2.0 (Jarvis Self-Learning Engine)" \
            --notes "Voice v2.0: Calibrated 1:1 Precision Tapping, 35px Magnetic Snap, Long-Press (700ms), 1-Shot Notes, Autonomous Learning Memory, and My Files Integration." || true
